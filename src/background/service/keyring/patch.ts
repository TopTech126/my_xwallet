/**
 * patch trezor-like, but which would lead typescript@4.x error on package @trezor/schema-utils/node_modules/@sinclair/typebox/typebox.d.ts
 * 
 * the patch migrated @trezor/connect-web now, see more details on 
 */
// import TrezorConnect from '@trezor/connect-web';

// import OneKeyConnect from '@onekeyfe/connect';

// (globalThis as any)._TrezorConnect = TrezorConnect;

// (globalThis as any)._OnekeyConnect = OneKeyConnect;
